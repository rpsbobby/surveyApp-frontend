import { AuthenticationBody } from './authentication-body';

describe('AuthenticationBody', () => {
  it('should create an instance', () => {
    expect(new AuthenticationBody()).toBeTruthy();
  });
});
